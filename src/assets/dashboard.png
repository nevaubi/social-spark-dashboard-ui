
// This would be a base64 encoded image, but since we can't add binary files directly,
// we'll need to use a placeholder. You should upload a dashboard image through the UI.
// For now, we'll use a placeholder and adjust the code to work with it.
